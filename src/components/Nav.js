import React from "react"
import {Link} from "react-router-dom"

export const Nav = (props) => {
    return <nav>
        <Link to = "/past">
            <div>
                Past Events
            </div>
        </Link>

        
    </nav>
}